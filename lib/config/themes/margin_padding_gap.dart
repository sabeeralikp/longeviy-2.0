import 'package:flutter/material.dart';

const gapYSmall = SizedBox(height: 4.0);
const gapYMedium = SizedBox(height: 8.0);
const gapYLarge = SizedBox(height: 16.0);

const gapXSmall = SizedBox(width: 4.0);
const gapXMedium = SizedBox(width: 8.0);
const gapXLarge = SizedBox(width: 16.0);

const navButtonPadding = EdgeInsets.symmetric(horizontal: 12, vertical: 12);
const double navButtonGap = 10;

const pageXTPadding = EdgeInsets.only(top: 24, right: 24, left: 24);

const containerBMargin = EdgeInsets.only(bottom: 12);
const containerAPadding = EdgeInsets.all(24);

BorderRadius containerBorderRadius = BorderRadius.circular(20);

const noPadding = EdgeInsets.zero;

const noVisualDensity = VisualDensity(horizontal: -4, vertical: -4);
